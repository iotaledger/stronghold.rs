initSidebarItems({"derive":[["RequestPermissions","Implements the [`VariantPermission`] for struct/ unions with PermissionValue(1). For enums, it implements [`ToPermissionVariants`], which creates an according new enum Permission with Unit variants, and implements [`VariantPermission`] by assigning a permission value for each variant. The permission value is the \"index\" in the enum as exponent for the power of 2, thus from top to bottom 1, 2, 4, 8... This enables using it within the [`CommunicationActor`] and allows the [`CommunicationFirewall`] to handle it."]],"fn":[["build_plain",""],["impl_permission",""],["impl_to_permissioned",""],["match_fields",""]]});